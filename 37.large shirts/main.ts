function make_t_shirt(size:string="large", message:string = "I Love Typescript") {
    console.log(`Making a "${size}" t-shirt with the message "${message}" printed on it.`)
}

    make_t_shirt();
    make_t_shirt("medium");
    make_t_shirt("small","Dive into coding");